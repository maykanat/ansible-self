---
- name: User Playbook
  hosts: amazon
  become: true
  become_method: sudo


  tasks:
    - name: Add the user 'johnd' with a specific uid and a primary group of 'admin'
      ansible.builtin.user:
        name: johnd
        comment: John Doe
        group: wheel
...

